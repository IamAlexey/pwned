apiVersion: v1
kind: Service
metadata:
  name: {{ include "pwned-workflow.fullname" . }}
  labels:
    {{- include "pwned-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pwned-workflow.selectorLabels" . | nindent 4 }}
