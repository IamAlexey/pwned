apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pwned-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "pwned-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pwned-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
